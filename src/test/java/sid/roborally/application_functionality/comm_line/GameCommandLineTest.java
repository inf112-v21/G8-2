package sid.roborally.application_functionality.comm_line;

import org.junit.Test;
import sid.roborally.game_mechanics.card.Card;
import sid.roborally.game_mechanics.card.CardDealer;

import java.util.ArrayList;

public class GameCommandLineTest {

    /**
     * <p>This test needs input, so should be commented out to not stop the testing.</p>
     *//*
    @Test
    public void canSelectCards() {
        CardDealer dealer = new CardDealer();
        ArrayList<Card> testCards = new ArrayList<>();
        testCards.addAll(dealer.dealCards(9));

        ArrayList<Card> selectedCards = GameCommandLine.getLocalCardSequenceInput(testCards);
        for(Card c : selectedCards) System.out.println(c.getName());
    }*/
}
